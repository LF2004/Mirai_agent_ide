/**
 * Shared app constants defined in plain JS so the project stays easy to read.
 */
export const APP_MODES = {
  AGENT: 'agent',
  PLAN: 'plan',
  ASK: 'ask'
};

export const DEFAULT_MODEL = 'gpt-4.1';
